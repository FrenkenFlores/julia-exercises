# Sum function with error handaling

Write a varargs function (variadic function) that returns the sum of the given arguments.
The function should be able to work with numbers, strings, tuples and lists.
If a string can not be parsed to convert it into a number, then the function must
show the index of the item. Else return the sum of all elements.
